--- a/package.json
+++ b/package.json
@@ -1,7 +1,7 @@
 {
   "name": "dias-laborales",
   "private": true,
-  "version": "3.2.1",
+  "version": "3.2.2",
   "type": "module",
   "scripts": {
     "dev": "vite --host 0.0.0.0",
--- a/package-lock.json
+++ b/package-lock.json
@@ -1,12 +1,12 @@
 {
   "name": "dias-laborales",
-  "version": "3.2.1",
+  "version": "3.2.2",
   "lockfileVersion": 3,
   "requires": true,
   "packages": {
     "": {
       "name": "dias-laborales",
-      "version": "3.2.1",
+      "version": "3.2.2",
       "dependencies": {
         "@capacitor/android": "^7.4.0",
         "@capacitor/app": "^7.0.0",
--- a/android/app/build.gradle
+++ b/android/app/build.gradle
@@ -8,8 +8,8 @@
         applicationId "com.lyjdesarrollos.diaslaborales"
         minSdkVersion rootProject.ext.minSdkVersion
         targetSdkVersion rootProject.ext.targetSdkVersion
-        versionCode 10
-        versionName "3.2.1"
+        versionCode 11
+        versionName "3.2.2"
         testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
         aaptOptions {
             ignoreAssetsPattern '!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~'
--- a/src/core/notificationPlan.js
+++ b/src/core/notificationPlan.js
@@ -4,9 +4,6 @@
 
 export const WORK_CHANNEL = 'work-reminders';
 export const EVENT_CHANNEL = 'event-reminders';
-
-export const WORK_ALERT_GRACE_MS = 15 * 60_000;
-export const WORK_ALERT_RECOVERY_DELAY_MS = 5_000;
 
 function weekdayAndDay(date) {
   return date.toLocaleDateString('es-AR', { weekday: 'long', day: 'numeric' });
@@ -29,26 +26,17 @@
       0,
       0
     );
-    let scheduledAt = notifyDate;
-    let lateRecovery = false;
-    if (notifyDate <= now) {
-      const lateness = now.getTime() - notifyDate.getTime();
-      if (lateness > WORK_ALERT_GRACE_MS) continue;
-
-      // Si la app se abre justo en el minuto del aviso, o Android demora una alarma
-      // flexible, no descartamos el turno de mañana por unos pocos segundos/minutos.
-      // Lo recuperamos inmediatamente para evitar el caso de "02:45:20 -> ya pasó".
-      scheduledAt = new Date(now.getTime() + WORK_ALERT_RECOVERY_DELAY_MS);
-      lateRecovery = true;
-    }
+    // Un turno produce un solo aviso. Si la hora ya pasó, no se genera una
+    // segunda notificación de "recuperación" al abrir la app.
+    if (notifyDate <= now) continue;
 
     notifications.push({
       id: workNotificationId(workDate),
       title: 'Mañana trabajás',
       body: `Tu próximo turno es mañana, ${weekdayAndDay(workDate)}.`,
       channelId: WORK_CHANNEL,
-      schedule: { at: scheduledAt, allowWhileIdle: true },
-      extra: { owner: 'dias-laborales-v2', type: 'work', dateKey: dateKey(workDate), lateRecovery }
+      schedule: { at: notifyDate, allowWhileIdle: true },
+      extra: { owner: 'dias-laborales-v2', type: 'work', dateKey: dateKey(workDate) }
     });
   }
   return notifications;
--- a/android/app/src/main/java/com/lyjdesarrollos/diaslaborales/WorkAlarmScheduler.java
+++ b/android/app/src/main/java/com/lyjdesarrollos/diaslaborales/WorkAlarmScheduler.java
@@ -17,7 +17,6 @@
     static final String KEY_CLICK_TYPE = "pending_click_type";
     static final String ACTION_WORK_ALARM = "com.lyjdesarrollos.diaslaborales.WORK_ALARM";
     static final String CHANNEL_ID = "work-reminders-native";
-    static final long GRACE_MS = 15L * 60L * 1000L;
 
     private WorkAlarmScheduler() {}
 
@@ -38,13 +37,7 @@
                 JSONObject spec = normalizeSpec(source);
                 long triggerAt = spec.optLong("triggerAt", 0L);
                 if (triggerAt <= 0L) continue;
-                if (triggerAt <= now) {
-                    long lateBy = now - triggerAt;
-                    if (lateBy > GRACE_MS) continue;
-                    triggerAt = now + 5_000L;
-                    spec.put("triggerAt", triggerAt);
-                    spec.put("lateRecovery", true);
-                }
+                if (triggerAt <= now) continue;
                 scheduleOne(context, spec);
                 stored.put(spec);
                 scheduled++;
@@ -70,14 +63,7 @@
             if (spec == null) continue;
             long triggerAt = spec.optLong("triggerAt", 0L);
             if (triggerAt <= 0L) continue;
-            if (triggerAt <= now) {
-                long lateBy = now - triggerAt;
-                if (lateBy > GRACE_MS) continue;
-                try {
-                    spec.put("triggerAt", now + 5_000L);
-                    spec.put("lateRecovery", true);
-                } catch (JSONException ignored) {}
-            }
+            if (triggerAt <= now) continue;
             scheduleOne(context, spec);
             kept.put(spec);
             scheduled++;
--- a/src/services/notifications.js
+++ b/src/services/notifications.js
@@ -108,8 +108,8 @@
     if (!LocalNotifications) return;
     const pending = await LocalNotifications.getPending();
     const toCancel = (pending.notifications || []).filter(item =>
-      item?.extra?.type === 'event' || item?.extra?.type === 'test' ||
-      (item?.extra?.owner === 'dias-laborales-v2' && item?.extra?.type !== 'work')
+      item?.extra?.owner === 'dias-laborales-v2' ||
+      item?.extra?.type === 'work' || item?.extra?.type === 'event' || item?.extra?.type === 'test'
     );
     if (toCancel.length) {
       await LocalNotifications.cancel({ notifications: toCancel.map(item => ({ id: item.id })) });
@@ -135,6 +135,21 @@
   }
 
   await ensureChannels();
+
+  // Limpieza de migración: versiones anteriores pudieron dejar avisos de turnos
+  // programados con LocalNotifications. Se cancelan para que sólo quede el
+  // AlarmManager nativo y cada turno genere exactamente una notificación.
+  const LocalNotifications = await getLocalNotifications();
+  if (LocalNotifications) {
+    const pendingBeforeNative = await LocalNotifications.getPending();
+    const staleWork = (pendingBeforeNative.notifications || []).filter(item =>
+      item?.extra?.type === 'work' ||
+      (item?.extra?.owner === 'dias-laborales-v2' && item?.extra?.type === 'work')
+    );
+    if (staleWork.length) {
+      await LocalNotifications.cancel({ notifications: staleWork.map(item => ({ id: item.id })) });
+    }
+  }
 
   // Los avisos de turnos se entregan con AlarmManager + BroadcastReceiver nativos.
   // Una vez programados no dependen del WebView, JavaScript ni de que la Activity siga viva.
@@ -154,7 +169,6 @@
   }
 
   // Las anotaciones siguen usando el plugin oficial; no duplicamos los turnos allí.
-  const LocalNotifications = await getLocalNotifications();
   let eventScheduled = 0;
   if (LocalNotifications) {
     const pending = await LocalNotifications.getPending();
--- a/src/main.js
+++ b/src/main.js
@@ -24,7 +24,7 @@
 } from './core/schedule.js';
 import { deleteEvent, reminderValueForEvent, upsertEvent } from './core/events.js';
 import { clearStoredState, loadState, saveState } from './services/storage.js';
-import { applyNativeStatusBar, getAppPlugin, hideNativeSplash, isNativeAndroid, openMercadoPagoApp, openExternalUrl, openNativeAppSettings } from './services/native.js';
+import { applyNativeStatusBar, getAppPlugin, hideNativeSplash, isNativeAndroid, openExternalUrl, openNativeAppSettings } from './services/native.js';
 import {
   cancelManagedNotifications,
   consumeNativeWorkNotificationClick,
@@ -179,7 +179,6 @@
       renderEverything();
       applyDemoViewIfRequested();
       refreshNotificationUi().catch(error => console.error('No se pudo actualizar el diagnóstico:', error));
-      scheduleRefresh('startup');
     }
 
     consumePendingNotificationAction(handleNotificationAction);
@@ -309,16 +308,12 @@
     if (!opened) showToast('No se pudo abrir la precisión de alarmas.', 'info');
   });
   byId('test-notification').addEventListener('click', testNotificationNow);
-  byId('open-support').addEventListener('click', () => {
-    byId('support-dialog')?.showModal();
-  });
-  byId('support-close').addEventListener('click', () => byId('support-dialog').close());
-  byId('open-lyj-site').addEventListener('click', async () => {
+  document.querySelectorAll('[data-support-link]').forEach(button => button.addEventListener('click', async () => {
+    if (!await openExternalUrl('https://mpago.la/1oYkso5')) showToast('No se pudo abrir el link de colaboración.', 'error');
+  }));
+  document.querySelectorAll('[data-lyj-link]').forEach(button => button.addEventListener('click', async () => {
     if (!await openExternalUrl('https://www.lyjdesarrollos.com')) showToast('No se pudo abrir LyJ Desarrollos.', 'error');
-  });
-  byId('support-lyj-site').addEventListener('click', async () => {
-    if (!await openExternalUrl('https://www.lyjdesarrollos.com')) showToast('No se pudo abrir LyJ Desarrollos.', 'error');
-  });
+  }));
   byId('export-data').addEventListener('click', exportBackup);
   byId('import-data').addEventListener('click', () => byId('import-file').click());
   byId('import-file').addEventListener('change', importBackupFromFile);
--- a/index.html
+++ b/index.html
@@ -48,6 +48,16 @@
           <span class="notification-dot" aria-hidden="true"></span>
         </button>
       </header>
+      <aside class="project-promo" aria-label="Colaboración y desarrollo">
+        <button class="promo-action promo-support" type="button" data-support-link>
+          <span class="promo-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M12 20.2 4.8 13.4a4.7 4.7 0 0 1 6.6-6.7L12 7.3l.6-.6a4.7 4.7 0 0 1 6.6 6.7Z"/></svg></span>
+          <span><strong>Apoyá Días Laborales</strong><small>Colaborá con el proyecto</small></span>
+        </button>
+        <button class="promo-action promo-lyj" type="button" data-lyj-link>
+          <span class="promo-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M14 5h5v5M19 5l-8 8"/><path d="M12 5H6a2 2 0 0 0-2 2v11a2 2 0 0 0 2 2h11a2 2 0 0 0 2-2v-6"/></svg></span>
+          <span><strong>LyJ Desarrollos</strong><small>lyjdesarrollos.com</small></span>
+        </button>
+      </aside>
 
       <main class="main-content">
         <section id="view-home" class="view active" data-view="home" aria-labelledby="home-title">
@@ -179,46 +189,9 @@
             <button id="delete-data" class="danger-link" type="button">Borrar todos los datos</button>
           </section>
 
-          <section class="settings-section support-section" aria-labelledby="support-title">
-            <div class="support-banner">
-              <div class="support-badge" aria-hidden="true">
-                <svg viewBox="0 0 24 24"><path d="M12 21s-7.5-4.4-9.4-9.1C1 8 3.2 4.5 6.8 4.2c2-.2 3.7.8 5.2 2.5 1.5-1.7 3.2-2.7 5.2-2.5 3.6.3 5.8 3.8 4.2 7.7C19.5 16.6 12 21 12 21Z"/></svg>
-              </div>
-              <div class="support-copy">
-                <p class="section-kicker">APOYÁ EL PROYECTO</p>
-                <h2 id="support-title">Ayudá a que Días Laborales siga creciendo</h2>
-                <p>La app es gratuita, sin publicidad y privada. Si te sirve, podés colaborar de forma voluntaria con su desarrollo.</p>
-              </div>
-              <button id="open-support" class="support-button" type="button">
-                <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 3v18M7.5 7.5C7.5 5.6 9.3 4 12 4s4.5 1.4 4.5 3.5c0 5-9 2.3-9 7 0 2.1 1.8 3.5 4.5 3.5s4.5-1.6 4.5-3.5"/></svg>
-                <span><strong>Quiero colaborar</strong><small>Ver opciones de colaboración</small></span>
-                <svg class="support-arrow" viewBox="0 0 24 24" aria-hidden="true"><path d="m9 18 6-6-6-6"/></svg>
-              </button>
-              <p class="support-note">La colaboración es opcional y no desbloquea funciones.</p>
-            </div>
-          </section>
-
-          <section class="settings-section developer-section" aria-labelledby="developer-title">
-            <p class="section-kicker">DESARROLLO</p>
-            <h2 id="developer-title">LyJ Desarrollos</h2>
-            <p>Días Laborales es desarrollada y mantenida por LyJ Desarrollos.</p>
-            <button id="open-lyj-site" class="developer-link" type="button">
-              <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3c2.4 2.5 3.6 5.5 3.6 9S14.4 18.5 12 21c-2.4-2.5-3.6-5.5-3.6-9S9.6 5.5 12 3Z"/></svg>
-              <span><strong>www.lyjdesarrollos.com</strong><small>Conocé nuestros proyectos y servicios</small></span>
-              <svg class="support-arrow" viewBox="0 0 24 24" aria-hidden="true"><path d="m9 18 6-6-6-6"/></svg>
-            </button>
-          </section>
         </section>
       </main>
 
-      <dialog id="support-dialog" class="app-dialog support-dialog">
-        <form method="dialog" class="dialog-card">
-          <div class="dialog-head"><div><p class="section-kicker">COLABORAR</p><h2>Gracias por apoyar Días Laborales</h2></div><button id="support-close" class="icon-button" type="button" aria-label="Cerrar"><svg viewBox="0 0 24 24"><path d="m6 6 12 12M18 6 6 18"/></svg></button></div>
-          <p>Tu QR de Naranja X es interoperable, pero no es un enlace web. Para que el botón pueda abrir directamente una pantalla de pago desde el mismo teléfono hace falta un <strong>Link de pago</strong> de Mercado Pago o Naranja X.</p>
-          <div class="support-info-box"><strong>No mostramos el QR</strong><span>Cuando agreguemos el Link de pago, este mismo botón llevará directo al cobro.</span></div>
-          <button id="support-lyj-site" class="primary-button" type="button">Visitar LyJ Desarrollos</button>
-        </form>
-      </dialog>
 
       <nav class="bottom-nav" aria-label="Navegación principal">
         <button class="nav-item active" type="button" data-view-target="home"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="m4 10 8-6 8 6v9a1 1 0 0 1-1 1h-5v-6h-4v6H5a1 1 0 0 1-1-1Z"/></svg><span>Inicio</span></button>
--- a/src/styles/app.css
+++ b/src/styles/app.css
@@ -418,3 +418,25 @@
 .support-dialog .dialog-card { max-width: 380px; }
 .support-info-box { display: flex; flex-direction: column; gap: 4px; margin: 14px 0; padding: 12px 14px; border-radius: 14px; background: color-mix(in srgb, var(--accent) 8%, var(--surface)); border: 1px solid color-mix(in srgb, var(--accent) 25%, var(--line)); }
 .support-info-box span { color: var(--text-muted); font-size: .75rem; line-height: 1.45; }
+
+
+/* v3.2.2 · colaboración visible en todas las secciones */
+.project-promo {
+  width: min(100%, 620px); margin: 10px auto 0; padding: 0 18px;
+  display: grid; grid-template-columns: 1.15fr .85fr; gap: 8px;
+}
+.promo-action {
+  min-width: 0; min-height: 52px; display: grid; grid-template-columns: 34px 1fr;
+  align-items: center; gap: 9px; padding: 8px 10px; border: 1px solid var(--line);
+  border-radius: 14px; color: var(--text); text-align: left; background: var(--surface);
+}
+.promo-support { border-color: color-mix(in srgb, var(--blue) 36%, var(--line)); background: color-mix(in srgb, var(--blue) 8%, var(--surface)); }
+.promo-icon { width: 34px; height: 34px; display: grid; place-items: center; border-radius: 10px; background: color-mix(in srgb, var(--sky) 12%, transparent); color: var(--sky); }
+.promo-icon svg { width: 20px; height: 20px; fill: none; stroke: currentColor; stroke-width: 1.9; stroke-linecap: round; stroke-linejoin: round; }
+.promo-action > span:last-child { display:flex; flex-direction:column; gap:2px; min-width:0; }
+.promo-action strong { font-size:.72rem; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
+.promo-action small { color:var(--text-muted); font-size:.57rem; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
+@media (max-width: 380px) {
+  .project-promo { grid-template-columns: 1fr; }
+  .promo-action { min-height: 48px; }
+}
--- a/tests/notificationPlan.test.js
+++ b/tests/notificationPlan.test.js
@@ -38,27 +38,13 @@
 });
 
 
-test('si el aviso de mañana venció hace pocos segundos, lo recupera en vez de descartarlo', () => {
+test('un aviso ya entregado no se vuelve a programar al abrir la app segundos después', () => {
   const state = baseState({
     startDate: '2027-01-02',
     schemeSegments: [{ from: '2027-01-02', anchorIsWork: true }],
     notifications: { enabled: true, hour: 22, minute: 0, horizonDays: 30 }
   });
   const now = new Date(2027, 0, 1, 22, 0, 20, 0);
-  const plan = buildWorkNotificationPlan(state, now);
-  const alert = plan.find(item => item.extra.dateKey === '2027-01-02');
-  assert.ok(alert);
-  assert.equal(alert.extra.lateRecovery, true);
-  assert.equal(alert.schedule.at.getTime(), now.getTime() + 5_000);
-});
-
-test('un aviso vencido hace más de quince minutos no se recupera', () => {
-  const state = baseState({
-    startDate: '2027-01-02',
-    schemeSegments: [{ from: '2027-01-02', anchorIsWork: true }],
-    notifications: { enabled: true, hour: 22, minute: 0, horizonDays: 30 }
-  });
-  const now = new Date(2027, 0, 1, 22, 16, 0, 0);
   const plan = buildWorkNotificationPlan(state, now);
   assert.equal(plan.some(item => item.extra.dateKey === '2027-01-02'), false);
 });
